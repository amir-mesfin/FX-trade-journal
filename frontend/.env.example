# Copy to `.env` in this folder — Vite loads `.env` automatically (restart dev server after edits).
#
# Backend URL when the SPA is hosted separately (e.g. Netlify/Vercel/Render static).
# Leave empty for local dev (Vite proxies /api → localhost:5000).
#

VITE_API_URL=
